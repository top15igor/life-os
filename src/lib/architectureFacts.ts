// ===== Знания об устройстве проекта — ТОЛЬКО для владельца (Игоря). =====
// Подмешиваются в промпты ассистента и AI-друга исключительно когда вопрос
// задаёт владелец: обычным пользователям бот об инфраструктуре не рассказывает.
// Текст дублирует смысл страницы /admin/architecture — при крупных изменениях
// инфраструктуры обновлять оба места.

export const OWNER_ID = "00000000-0000-0000-0000-000000000000";

export function isOwner(userId: string): boolean {
  return userId === OWNER_ID;
}

export function architectureFacts(): string {
  return `УСТРОЙСТВО ПРОЕКТА LIFE OS (владельческая справка; полная версия — страница /admin/architecture):

ТРИ СЕРВИСА И ИХ РОЛИ:
• Vercel — исполняет код: сайт life-os.today, бот (вебхук /api/telegram), все API, крон. Serverless, само масштабируется.
• Supabase — облачный PostgreSQL, хранит ВСЕ данные: записи, аккаунты, финансы, задачи, базу знаний, фото-память. На компьютере владельца данных НЕТ.
• GitHub — исходный код (репозиторий top15igor/life-os, публичный). Push в main → авто-деплой на Vercel за ~2 минуты, кнопок жать не нужно.

КОД vs ДАННЫЕ: код существует в трёх синхронных копиях (ноутбук ~/Documents/Claude/Projects/life-os, GitHub, Vercel); данные — только в Supabase.

СТЕК: Next.js 15 (App Router) + React 19 + TypeScript; Telegram Bot API (webhook); AI: Claude Sonnet 4.6 (разбор записей, ассистент, AI-друг), Claude Haiku 4.5 (роутер сообщений, утренние пуши), OpenAI Whisper (голос → текст). 5 языков (RU/EN/UK/FR/ES).

ПУТЬ СООБЩЕНИЯ: голос/текст → /api/telegram на Vercel → Whisper (если голос) → Haiku-роутер решает «действие / вопрос / запись» → Sonnet разбирает или отвечает → всё сохраняется в Supabase → бот подтверждает.

СЕКРЕТЫ: все ключи (Anthropic, OpenAI, Telegram, Supabase) лежат в переменных окружения Vercel с пометкой Sensitive — их нельзя выгрузить наружу даже владельцу (только заменить новыми). В коде и репозитории ключей нет.

ЛОГИ: логи прода видны в Vercel; на текущем бесплатном тарифе хранятся ~1 час — ошибку пользователя нужно ловить «по горячему» (повторить действие и смотреть логи).

РЕЗЕРВНЫЕ КОПИИ:
• Каждое воскресенье — автодамп ВСЕЙ базы (все таблицы, все пользователи) в .zip с JSON-файлами, приходит владельцу в Telegram (крон). Ручной запуск: открыть /api/admin/dbdump залогиненным владельцем. Восстановление: JSON вставляется обратно в Supabase.
• 1-го числа каждого месяца — каждому пользователю его личный дневник в .zip (Obsidian-vault, Markdown).
• Экспорт всех своих данных доступен каждому пользователю в один клик (/api/export).
• Автобэкапов на стороне Supabase на бесплатном тарифе нет (ежедневные копии — на платном Pro).

БЕЗОПАСНОСТЬ: RLS на всех таблицах; к базе ходит только серверный ключ; каждый эндпоинт жёстко фильтрует по user_id владельца данных; вход по личной ссылке (magic-link) + опциональный PIN; открытый код ≠ открытый доступ (ключей в коде нет, деплоит только владелец).

ЭКОНОМИКА (грубо): Whisper ≈ $0.006/мин голоса; разбор записи Sonnet — единицы центов; Haiku-роутер — доли цента; хостинг пока на бесплатных тарифах. Учёт расходов на AI — карточка лимитов в /admin, prompt caching включён на частых вызовах.

АДМИНКА (/admin, только владелец): метрики (пользователи, активность, возвраты), рост и удержание, обратная связь, PMF-опрос (/admin/pmf), каналы привлечения (/admin/channels), отчёты тестировщиков (/admin/tests), бэклог идей (/admin/tasks), архитектура (/admin/architecture), маркетинг-материалы.`;
}
