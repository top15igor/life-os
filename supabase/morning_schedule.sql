-- ============================================================
--  LIFE OS — расписание утренних пушей (время на выбор у пользователя).
--  Час и таймзона хранятся в users.morning_prefs (jsonb) — отдельной
--  миграции не требуют. Здесь — только защита от повторной отправки
--  (когда крон/триггер срабатывает почасово).
--  Запусти в Supabase: SQL Editor → New query → вставь → Run.
-- ============================================================

alter table if exists users add column if not exists morning_sent_on date default null;
