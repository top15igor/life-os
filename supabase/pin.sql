-- ============================================================
--  LIFE OS — PIN-замок на веб-доступ (опционально для пользователя)
--  Запусти в Supabase: SQL Editor → New query → вставь → Run.
-- ============================================================

alter table users add column if not exists pin_hash text;
